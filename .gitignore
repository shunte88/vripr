# Build output
/target

nextsess
bump.sh
# Cargo lock (keep for binaries, discard for libraries — this is a binary)
# Cargo.lock

# Editor temporaries
*.swp
*.swo
*.orig
*~

# IDE
.vscode/
.idea/
*.iml

# macOS
.DS_Store
**/.DS_Store

# Test / scratch audio files
*.wav
*.flac
*.mp3
*.ogg
!/assets/**

# Temporary analysis WAV (written to /tmp but just in case)
/tmp/vripr_analysis_*.wav

# Claude Code local config
.claude/

# Environment / secrets
.env
*.env.local

# Config override for local dev (real config lives in ~/.config/vripr/)
vripr.toml
